# YeonsuBot 환경변수 예시
# 파일 복사: cp .env.example .env
# 그 다음 docker compose up -d

# 설정 파일(settings.json) 저장 경로. Docker 볼륨 기본값은 /data
SETTINGS_DIR=/data

# FastAPI 바인딩
HOST=0.0.0.0
PORT=8000

# 브라우저 headless 모드 (로컬 디버깅 시 false로 설정하면 브라우저 창 표시)
HEADLESS=true
